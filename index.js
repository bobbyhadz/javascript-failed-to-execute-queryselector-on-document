// ✅ Works
const first = document.querySelector("[id='1box']");
console.log(first); // 👉️ div#1box

// However, it is better to change the `id` of the element to NOT start with a digit (as shown in the article)
